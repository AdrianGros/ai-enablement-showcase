# Material Inventory

Reviewed source scope:

- `/opt`
- `/tmp`

Relevant candidates found in accessible paths:

1. `/opt/rolldrauf-bot-dev/docs/rolldrauf_commands_table.pdf`
2. `/opt/rolldrauf-bot-dev/docs/rolldrauf_commands_table.ps`
3. `/opt/rolldrauf-bot-dev/docs/rolldrauf_commands_table.tbl`
4. bot and infra markdown documentation under `/opt/rolldrauf-bot-dev/docs/`, `/opt/rolldrauf-bot-prod/docs/`, and `/opt/rolldrauf-infra/notes/`

Assessment:

- none of the discovered artifacts are clearly AI training presentations
- the PDF candidate is a command reference, not an AI education artifact
- the markdown material is project and operations documentation, not learner-facing AI material

No artifacts were copied into `presentations/` for this milestone.
